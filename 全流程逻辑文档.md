# 全流程逻辑文档 — AI 短剧自动化生产流水线

> 版本：v3.0.0 | 最后更新：2026-03-03

---

## 系统总览

```
                小说文本
                   │
    ┌──────────────▼───────────────────────────────────────┐
    │              PipelineOrchestrator                     │
    │  (core/pipeline.py + core/stages/ Stage 体系)         │
    └──────────────┬───────────────────────────────────────┘
                   │ 共享 PipelineContext (数据令牌)
    ┌──────────────▼──────────────────────┐
    │  Stage 1: 记忆提取 MemoryExtractionStage  │
    ├──────────────┬──────────────────────┤
    │  Stage 1.5:  │ 视觉资产 VisualAssetStage  │
    ├──────────────┼──────────────────────┤
    │  Stage 2:    │ 分镜生成 StoryboardStage   │
    ├──────────────┼──────────────────────┤
    │  Stage 3:    │ 视频派发 VideoDispatchStage │
    ├──────────────┼──────────────────────┤
    │  Stage 4:    │ 视频生成 VideoGenStage      │
    ├──────────────┼──────────────────────┤
    │  Stage 5:    │ 后期混流 PostMuxStage       │
    └─────────────────────────────────────┘
                   │
             成片 MP4 归档
```

---

## Stage 0 — 任务入口（FastAPI）

**文件**：`web/routes.py` → `core/pipeline.py:process_novel_to_feishu()`

**触发方式**：
- 前端 WebUI 上传 txt 文件 → POST `/api/upload_novel`
- 批量接口：POST `/api/upload_novel_batch`
- 后台视频生成（独立触发）：POST `/api/run_video_generation`

**输入参数**：
- `novel_text`：小说正文（UTF-8 字符串，支持任意长度）
- `novel_id`：项目标识（唯一，对应飞书数据隔离主键）
- `style_key`：画风预设（anime / realistic / manhwa …）
- `gateway`：视频网关（wan_2_6 / seedance-1.5-pro）
- `sandbox_mode`：沙盒标记（True=不实际提交视频，用于调试）

---

## Stage 1 — 记忆提取 (MemoryExtractionStage)

**目标**：从小说文本中抽取稳定的世界观/角色实体，建立「记忆中枢」。

### 流程

```
novel_text
   │
   ├── [首次] DeepSeek → memory_init_extract.j2
   │         → 全量实体建档（角色/世界观/物品/关系…）
   │         → 写入飞书【记忆中枢表】
   │
   └── [复用] MemoryEngine.evolve_memory()
             → memory_delta_extract.j2（增量 diff）
             → 仅 upsert 新词条/更新过期词条
```

**输出到 PipelineContext**：
- `context.active_memories`：有效记忆词条列表（状态≠废弃）

**飞书写入**：【记忆中枢表】`name / lore / visual_aura / category / entity_id`

---

## Stage 1.5 — 视觉资产管线 (VisualAssetStage)

**目标**：为登场实体生成并固定视觉参考图，严格区分「三视图」与「分镜首帧」。

### 三路判断逻辑

```
for each entity in context.active_memories:

  Path A ─ 无现有素材
    │  Step 1: DeepSeek + visual_asset_turnaround.j2 → Image API
    │          → 三视图 turnaround_url（白背景，纯设定参考，❌ 不进 I2V）
    │  Step 2: DeepSeek + visual_asset_scene_frame.j2 → Image API
    │          → 首帧图 scene_frame_url（带场景，16:9，✅ I2V 入参）
    │  写入飞书【素材生成表】
    └  写回【剧本拆解表.首帧参考图】

  Path B ─ 有素材 + lore 变化
    │  复用旧 seed，重新生成首帧图（风格保持一致，场景更新）
    └  更新飞书

  Path C ─ 有素材 + 无变化
           透传现有 URL（0 次 API 调用）
```

**Prompt 分流**：

| 用途 | 模板 | 特点 |
|------|------|------|
| 三视图参考 | `visual_asset_turnaround.j2` | 纯白背景, A-pose, 多角度 |
| I2V 首帧图 | `visual_asset_scene_frame.j2` | 真实场景背景, 16:9, 动态姿势 |

**配额热重载**：`QUOTA_EXHAUSTED` 错误时自动等待 15min 后重试（StateKeeper）。

**输出到 PipelineContext**：
- `context.turnaround_urls`：`{entity_id: url}` ← 仅供设计审查
- `context.scene_frame_urls`：`{entity_id: url}` ← 传给 VideoGenStage

---

## Stage 2 — 分镜生成 (StoryboardStage)

**目标**：将小说切块，逐块生成结构化分镜场景。

### 流程

```
novel_text
   │ smart_chunk_text(chunk_size=1000)  ← 按语义切分，不截断句子
   │
for chunk in chunks:
   │  过滤本块相关记忆词条
   │  DeepSeek → stage2_entity_aware.j2（实体感知分镜生成）
   │  → scenes: [{visual_prompt, audio_prompt, camera, emotion, summary…}]
   │
   ├── rolling_context 维护最后 1 集的上下文（防止文风断层）
   └── all_scenes.extend(scenes)

stage3_global_audit.j2 → 全局一致性校验（可选）
写入飞书【剧本拆解表】（含旁白与台词、音效提示词字段）
```

**输出到 PipelineContext**：
- `context.all_scenes`：完整分镜列表
- `context.visual_prompts`：`[(prompt, image_url), ...]` 元组列表（image_url=scene_frame）

---

## Stage 3 — 视频派发 (VideoDispatchStage)

**目标**：将 `visual_prompts` 提交到云端视频生成队列，写回飞书【制片工厂表】。

### 网关路由

```python
GATEWAY_MODEL_MAP = {
    "wan_2_6":       → Wan2_6VideoAPI(model="wan2.6-i2v")
    "wan2.6-i2v-flash": → Wan2_6VideoAPI(model="wan2.6-i2v-flash", audio=False)
    "seedance-1.5-pro": → VolcengineVideoAPI(...)
}
```

**I2V 路径**（有 image_url）：
```
submit_task(prompt, image_url=scene_frame_url) → task_id → 写飞书
```

**T2V 降级**（无 image_url，例如场景首帧未生成）：
```
submit_task(prompt) → task_id → 写飞书（标注 T2V）
```

---

## Stage 4 — 视频生成监控 (VideoGenStage)

**目标**：轮询 Stage 3 提交的所有 task_id，等待 SUCCEEDED，下载到本地。

```
for task_id in context.pending_tasks:
    loop:
        status = check_status(task_id)
        if SUCCEEDED → download(video_url) → context.downloaded_video_paths.append(...)
        if FAILED    → context.errors.append(...)
        else         → sleep(10) && retry
```

**阿里云 wan2.6 有声视频**：成功时额外解析 `audio_url`（Stage5 可直接使用）。

---

## Stage 5 — 后期混流 (PostMuxStage)

**目标**：TTS + SFX 生成，FFmpeg 多轨合并，输出最终成片 MP4。

### 流程

```
for video_path in context.downloaded_video_paths:

  并发:
  ├── AudioOrchestrator.produce(scene)
  │     ├── TTS: 飞书.旁白与台词 → qwen3-tts-flash → .mp3
  │     └── SFX: 飞书.音效提示词 → Pollinations → .wav（非致命）
  │
  └── ffmpeg_service.mux(
          video=video_path,
          tts=tts_path,
          sfx=sfx_path,
          strategy="freeze_frame"   ← 首帧定格延长对齐TTS
      ) → {ep}_final.mp4

context.muxed_output_paths.append(result)
```

**TTS 三路优先级**：
1. 阿里云 qwen3-tts-flash（免费首选，复用 ALIYUN_API_KEY）
2. 火山引擎 TTS（需配置 VOLCENGINE_TTS_APP_ID）
3. Edge-TTS 兜底（无限免费，无需 Key）

**freeze_frame 对齐原理**：
- `ffprobe` 探测视频时长 vs 音频时长
- 若 TTS > 视频，`tpad=stop_mode=clone:stop_duration=X` 补最后一帧定格

---

## 数据流总览（PipelineContext 字段演进）

```
Stage 1 → active_memories          [记忆词条列表]
Stage 1.5 → turnaround_urls         [设定参考图, 不进I2V]
           → scene_frame_urls        [首帧图, I2V 入参]
Stage 2 → all_scenes               [分镜 JSON 列表]
         → visual_prompts           [(prompt, image_url) 元组]
Stage 3 → (写飞书制片工厂表，不存 context)
Stage 4 → downloaded_video_paths   [本地 .mp4 路径]
Stage 5 → muxed_output_paths       [成片 .mp4 路径]
通用    → errors                   [任意 Stage 写入的错误列表]
```

---

## 服务层依赖关系

```
LLM 层               图像层                 视频层               音频层
───────              ──────                 ──────               ──────
DeepSeekService      AliyunImageService     Wan2_6VideoAPI       TTSService
   │                   同步: wan2.6-t2i       I2V/T2V 12款          阿里云 qwen3-tts
   │                   异步: wanx/wan2.x       audio=True/False    火山引擎(可选)
   │                 FluxImageService         VolcengineVideoAPI  Edge-TTS(兜底)
   │                   flux-merged/dev/schnell  Seedance 系列
   │
MemoryEngine         PollinationsService    VideoGenStage        SFXService
   (增量记忆演化)       (图像/音效 免费)       (I2V/T2V 派发)        Pollinations(音效)
                                                                  静音兜底
```

---

## 飞书多维表格 Schema

| 表名 | App Token | 主要字段 |
|------|-----------|---------|
| 记忆中枢表 | `FEISHU_APP_TOKEN_MEMORY` | entity_id, name, lore, category, visual_aura |
| 素材生成表 | `FEISHU_APP_TOKEN_ASSETS` | entity_id, turnaround_url, scene_frame_url, seed |
| 剧本拆解表 | `FEISHU_APP_TOKEN_SCRIPT` | visual_prompt, 首帧参考图, 旁白与台词, 音效提示词 |
| 制片工厂表 | `FEISHU_APP_TOKEN_FACTORY` | task_id, status, video_url, novel_id |
