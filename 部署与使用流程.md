# 部署与使用流程 — AI 短剧自动化生产系统

> 版本：v3.0.0 | 适用系统：Windows 10/11 (64位)

---

## 快速开始（3分钟）

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 .env（仅需填写飞书 + 阿里云）
# 已内置默认飞书表，只需确认 ALIYUN_API_KEY 即可

# 3. 启动
双击 "一键启动WebUI.bat"
# 或：uvicorn web.app:app --host 0.0.0.0 --port 8000 --reload

# 4. 访问 WebUI
http://localhost:8000
```

---

## 环境要求

| 依赖 | 版本要求 | 说明 |
|------|---------|------|
| Python | ≥ 3.10 | 需支持 `str \| None` 类型语法 |
| pip 包 | 见 requirements.txt | `pip install -r requirements.txt` |
| Aria2c | 可选 | 多线程视频下载，性能提升 3-5x |
| FFmpeg | 可选 | 后期制作音视频混流（Stage 5）|

---

## A. 依赖安装

### Python 包
```bash
pip install -r requirements.txt
```

主要包说明：
- `fastapi` + `uvicorn`：WebAPI 服务器
- `aiohttp`：异步 HTTP（调用所有云端 API）
- `dashscope>=1.23.0`：阿里云 DashScope SDK（TTS/图像/视频）
- `edge-tts>=6.1.12`：微软 Edge TTS 兜底（无需 API Key）
- `ffmpeg-python>=0.2.0`：FFmpeg Python 包装（需宿主机安装 FFmpeg）

### FFmpeg（后期混流用，可选）
```bash
# Windows（推荐 winget）
winget install ffmpeg

# 验证
ffmpeg -version
```

### Aria2c（视频下载加速，可选）
```bash
# Windows 下载 aria2c.exe，放入 PATH 或项目根目录
# 测试
aria2c --version
```

---

## B. 环境变量配置（`.env`）

`.env` 文件已在项目根目录预配置，**仅需修改以下标注为必填的项**：

### 必填：飞书凭据
```ini
FEISHU_APP_ID=cli_xxx          # 飞书企业自建应用 App ID
FEISHU_APP_SECRET=xxx           # 飞书应用 Secret
```
> 飞书表格 Token 已预配置，无需修改。确保飞书应用已开通"多维表格"权限。

### 必填：阿里云百炼
```ini
ALIYUN_API_KEY=sk-xxx          # 一个 Key 覆盖：图像/视频/TTS/SFX 全部能力
```
获取地址：[阿里云百炼控制台](https://bailian.console.aliyun.com/) → 右上角 API-KEY

**此 Key 支持的免费能力**：

| 能力 | 模型 | 免费额度 |
|------|------|---------|
| 文生图 | wan2.6-t2i（同步直出） | 50张/90天 |
| 文生图 | wanx2.1-t2i-turbo | 500张/90天 |
| 文生图 | stable-diffusion-3.5-large | 500张（体验） |
| 图生视频 | wan2.6-i2v-flash | 50秒/90天 |
| 图生视频 | wanx2.1-i2v-turbo | 200秒/90天 |
| 文生视频 | wan2.6-t2v | 50秒/90天 |
| TTS 配音 | qwen3-tts-flash | 1万字符/90天 |

### 可选：火山引擎（视频+TTS）
```ini
# 视频：Seedance 系列（更高质量）
VOLCENGINE_API_KEY=xxx

# TTS：商业音色（火山引擎控制台 → 语音合成 → 应用管理）
# VOLCENGINE_TTS_APP_ID=xxx
# VOLCENGINE_TTS_ACCESS_TOKEN=xxx
```

### 可选：TTS 个性化
```ini
ALIYUN_TTS_VOICE=Cherry        # 默认音色（Cherry/Ethan/Serenity/Sunny/River）
ALIYUN_TTS_MODEL=qwen3-tts-flash   # 或 cosyvoice-v3.5-flash
EDGE_TTS_VOICE=zh-CN-XiaoxiaoNeural  # Edge-TTS 兜底音色
```

### 可选：企业微信通知
```ini
WX_BOT_WEBHOOK=https://qyapi.weixin.qq.com/...  # 任务完成时推送通知
```

---

## C. 启动服务

### Windows（推荐）
```
双击 "一键启动WebUI.bat"
```

### 命令行启动
```bash
# 开发模式（自动热重载）
uvicorn web.app:app --host 0.0.0.0 --port 8000 --reload

# 生产模式
uvicorn web.app:app --host 0.0.0.0 --port 8000 --workers 1
```
> ⚠️ 务必用 `--workers 1`，异步任务队列不支持多进程。

---

## D. WebUI 使用流程

### 1. 上传小说生成分镜
1. 访问 `http://localhost:8000`
2. 填写**项目名**（`novel_id`，英文+下划线，如 `wuxia_ep1`）
3. 上传 `.txt` 小说文件
4. 选择**画风**（anime / realistic / manhwa …）
5. 勾选**沙盒模式**（首次测试时建议勾选，不实际提交视频）
6. 点击**开始处理**→ 等待进度条完成

**结果**：飞书【剧本拆解表】自动填充 `visual_prompt`、`旁白与台词`、`音效提示词`。

### 2. 视觉资产生成（Stage 1.5 自动触发）
处理完分镜后，系统自动：
- 生成角色三视图（存入【素材生成表】）
- 生成各集首帧图（写回【剧本拆解表.首帧参考图】）

可在飞书对应表格中查看生成结果。

### 3. 发起视频生成
1. 在 WebUI 「视频生成」页签，选择 **novel_id**
2. 选择**视频网关**：
   - `wan_2_6`：阿里云 wan2.6-i2v（免费配额，推荐测试）
   - `wan2.6-i2v-flash`：极速I2V，首选
   - `seedance-1.5-pro`：火山引擎高质量（需配置 VOLCENGINE_API_KEY）
3. 点击**生成视频**→ 任务提交到云端异步队列

**进度查看**：
- WebUI 实时 SSE 进度流
- 飞书【制片工厂表】查看 task_id / status / video_url

### 4. 下载视频
任务 SUCCEEDED 后，视频 URL 写回飞书，同时下载到本地 `Download/` 目录。

### 5. 后期混流（Stage 5，需 FFmpeg）
```
安装 FFmpeg → winget install ffmpeg → 重启终端
```
混流自动触发，在 `Download/output/` 目录生成 `{novel_id}_ep001_final.mp4`。

---

## E. 批量处理

```bash
# 批量上传 API
POST /api/upload_novel_batch
Content-Type: multipart/form-data

files: [file1.txt, file2.txt, ...]
novel_id: my_project
style_key: anime
gateway: wan_2_6
```

**并发建议**：最多同时运行 **2 个任务**（内存 ≥512MB）。

---

## F. 调试与排障

### 查看实时日志
```bash
# 控制台直接显示 INFO 级别日志
# 增加 DEBUG 日志
uvicorn web.app:app --host 0.0.0.0 --port 8000 --log-level debug
```

### 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|---------|---------|
| `FEISHU_APP_ID is required` | .env 未正确加载 | 确认 `.env` 在项目根目录，Key 无多余空格 |
| `ALIYUN_API_KEY 未配置` | Key 未填写 | 在 `.env` 中填写 `ALIYUN_API_KEY=sk-xxx` |
| 图像生成返回 `QUOTA_EXHAUSTED` | 免费配额用尽 | 系统自动等待 15min 后重试，或切换模型 |
| `FFmpeg 未安装` | ffmpeg 不在 PATH | `winget install ffmpeg` 后重启终端 |
| 视频下载失败 | Aria2c 未运行 | 安装 aria2c 或忽略（降级 aiohttp 下载）|
| `DashScope Submit Error: 400` | API Key 错误或模型未开通 | 在百炼控制台确认模型已开通免费体验 |
| TTS 无声音 | 旁白与台词字段为空 | 在飞书【剧本拆解表】手动填写旁白，或等待 Stage 2 自动生成 |

### 沙盒模式测试
```python
# sandbox_mode=True：跳过真实图像/视频生成，仅测试流程
# 在 WebUI 勾选「沙盒模式」或 API 传参 sandbox_mode=true
```

### 手动测试单个 Stage
```python
# 在项目根目录运行
python scripts/_debug_fullpipeline.py
```

---

## G. 文件结构速览

```
12345678910-2.0.0/
├── .env                    # 环境变量配置（★ 必须先配置）
├── requirements.txt        # Python 依赖
├── 一键启动WebUI.bat        # Windows 一键启动脚本
├── 全流程逻辑文档.md         # 本系统架构与流程说明
├── 部署与使用流程.md         # 本文件
│
├── core/
│   ├── config.py           # 全局配置读取（从 .env 加载）
│   ├── pipeline.py         # 主流程编排（旧版兼容入口）
│   ├── stages/             # 解耦 Stage 体系（v2.8/v3.0）
│   │   ├── stage1_memory.py
│   │   ├── stage1_5_visual_asset.py
│   │   ├── stage2_storyboard.py
│   │   ├── stage3_video_dispatch.py
│   │   ├── stage4_video_gen.py
│   │   └── stage5_post_mux.py
│   └── services/
│       ├── aliyun_model_registry.py  # 全量模型注册表
│       ├── image_service/            # wan2.6/FLUX/Pollinations
│       ├── video_service/            # Wan2.6/Volcengine + 工厂函数
│       ├── llm_service/              # DeepSeek + MemoryEngine
│       ├── db_service/               # 飞书 Bitable CRUD
│       └── post_production/          # TTS + SFX + FFmpeg 混流
│
├── prompts/               # 10 个 Jinja2 提示词模板
├── Download/              # 视频下载目录
│   └── output/            # 成片输出目录
└── scripts/               # 飞书迁移、调试脚本
```
