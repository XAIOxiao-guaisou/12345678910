# 闪电解文架构设计与工作流梳理 (v2.0.0)

## 1. 整体架构 (架构 2.0: 异步事件驱动)
本系统采用完全解耦的异步事件驱动架构，以**飞书多维表格（Bitable）为主控消息队列和状态机**，将极其耗时的“AI内容生成”与“视频渲染下载”彻底从网页请求的同步阻塞中解放出来。

### 核心流向：
1. **API 层 (FastAPI)**：快速响应前端请求，完成高密度的脚本预处理和文本智能拆解，将任务推入云端队列后立即返回给用户成功响应，不发生阻塞。
2. **云端状态机 (飞书Bitable)**：作为轻量级的数据库与任务队列，所有拆解出的分镜和人物设定作为记录行（Record）写入表单，自带实时进度条展示界面。
3. **守护进程 (Worker Daemon)**：常驻系统后台的异步长连接进程。主动轮询飞书表格里处于 `["待生成"]` 状态的任务，并发执行耗时的网络渲染视频请求，最终自动将 MP4 落盘并直传回飞书，完成闭环管理。

---

## 2. 核心工程流 (Pipeline Workflow)
### 阶段一：提交即忘 (Web -> DeepSeek -> 飞书)
1. 客户端通过 `POST /api/upload_novel` 发送小说原文（支持长文本）。
2. FastAPI (`web.app`) 接管，立刻启动后台任务 `pipeline.process_novel_to_feishu`，并向客户端返回 200 OK 请求接受响应。
3. `pipeline` 内部逻辑：
   - 清理飞书对应表格历史脏数据（按批次安全删除）。
   - 调用 DeepSeek 提取“记忆中枢”（包含所有出场人物、世界观等），写入全局记忆表。
   - 对长文本进行滑动窗口切块 `smart_chunk_text`。
   - 将各个小块交由 DeepSeek 分析，推演并严格按照字数限制拆解成**高级分镜提示词**（包含视觉逻辑词、音频台词提示等）。
   - 组装提取后的多个场景结构，自动调用 `feishu_bitable.py` 将它们插入【飞书剧本分镜表】，且状态字段统一置为数组格式 `["待生成"]` 控制位。

### 阶段二：后台异步消费与异常闭环 (Worker -> Video Model -> 飞书)
1. `worker.py` 在启动时扫描飞书中是否有挂在半空中的 `["生成中"]` 状态的“僵尸任务”，如果有则重置为 `["待生成"]`，实现天然自愈。
2. Worker 内核不断轮询飞书执行 `get_pending_tasks(状态=["待生成"])`。
3. 一旦发现新任务，Worker 会为每个任务直接拉起一个并发协程，并将该行记录在云端立即锁定为 `["生成中"]`，防止多进程重复消费同一行记录。
4. 调用底层视频网关模型（如 Volcengine / Aliyun）提交排队。
5. 通过非阻塞（`asyncio`）机制每 10 秒轮询一次第三方视频生成的渲染 API 等待返回结果。
6. **成功回调**：视频生成完毕后，Worker 调用下载机制存放至本地临时 `Download/` 文件夹。
7. **自动化网盘打通**：将下载后的 `.mp4` 文件根据体积大小决定策略极速切片并上传（大文件采用安全的分步分大块接口机制）至飞书作为云附件回填到指定格子，随后更新状态为 `["已完成"]`。
8. **内存释放**：成功推送后，Worker 清空该片段本地磁盘留存文件，极严苟地进行容量规划避免机器磁盘撑爆。
9. **熔断与日志回弹**：如果在调用第三方接口期间遇到类似 5xx/10054 超时风暴，Tenacity 插件会进行自动**指数退避重试**防御打穿底层网络；若彻底重试报错捕获到了 `Exception`（譬如频繁违规等），Worker 会主动将这条任务平滑降级为 `["生成异常"]`，并将真实的 `Error` Traceback 或 String 直接作为文本写入飞书专属的【异常日志】列内，辅助排查。

---

## 3. 核心文件说明 (Detailed File Directory)

### 入口与调度脚本
- **`web/app.py`**：FastAPI 的主入口。定义了启动上下文事件、挂载静态文件访问（如根目录 `/` 返回 `index.html`）、及异常收口等路由控制基础。
- **`core/api/routes.py`**：专门处理 RESTful 接口逻辑（如主要的 `/api/upload_novel`）。负责衔接 Web 参数（剧本、设定等）与底层业务类的串通，并将其派发至 fastapi 内置的 `BackgroundTasks` 挂载背景任务栈。
- **`core/pipeline.py`**：核心流程的大管家编排枢纽。负责第一部分“文字推演”到“状态成表入云”的整个生命周期流（清洗 -> 构建记忆网络 -> 智能切段 -> 推送飞书队列）。
- **`worker.py`**：**（全新 2.0 增补）** 后台云附件及多模态轮询守护进程核心逻辑。管理整个长效事件循环网络，分配异步并发资源并进行视频合成操作、错误抛回、容错熔断收尾工作。
- **`一键启动WebUI.bat`**：一键并行部署测试工具箱。使用自带的 DOS `start` 并行命令树机制，可以在开发本地一瞬间搭建、并互不干扰地独立分离启动出 Uvicorn (Web 主体服务 API 进程) 和 Worker 控制台窗口。

### 服务层 (Services)
- **`core/services/db_service/feishu_bitable.py`**
  系统数据流与中间件全权大管家。负责全面封装和对接外部系统的飞书 OpenAPI。包含鉴权缓存令牌续约、大量历史单删隐患修复至安全的极速 `batch_delete` 结合治理机制、按状态进行精准多选单查 (`get_pending_tasks`)，以及支持了最智能的动态云端路由（**`<20MB` 基础附件接口短平快直推**，和 **`>=20MB` 按极严标准的规范强制 `4MB` 切片切分极速全量上传校验机制**）全效满足长视频、大体积视频的高效传输存储及 Bitable 嵌套附件绑定操作。
- **`core/services/llm_service/deepseek_service.py`**
  深度推理思维大模型对接的唯一交互类组件。包含向云端分发文字任务、对长本文进行分词切断、精准词典分析的预警（如针对字数进行分割重叠设计的 `smart_chunk_text`），为了抵挡不可预计的逆向网络高峰、抽风并发报错，外层都直接以 `Tenacity` 框架封装了精确防网络抖动的错误回滚及弹性重试等待包裹体系。
- **`core/services/video_service/`系列多服务文件**
  专门针对多家模型底座厂商（Aliyun、Volcengine火山引擎 等等）的视频直达网关实现标准层规范基类。`factory.py` 提供工厂单例机制负责动态路由和初始化分发策略，隔离掉了具体服务的复杂调用污染业务层，各个派生实例组件如 `aliyun_service.py` 等只用干净的解决自身厂商自有的 `task_id` 获取、请求签名鉴权和异步 `check_status` 等异步网络轮询检查和 URL 回传。

### 配置与基盘
- **`core/config.py`**：主要掌管读取项目的全局环境变量。基于新版 `python-dotenv` 依赖进行强类型装载，在执行系统初始化时会动态感知提取和将所有从 `.env` 配置文件解析出来的外购云认证密钥（如 `AK / SK / Bearer Token`等）打成 `settings` 单例配置供全局下沉服务零阻碍共享。
- **`.env.template`**：当前工程系统所极其依赖的外置环境变量备忘录与指南，详细阐明和提供了各项系统关键密钥的来源申请平台官网途径、默认的有效代理配置，以及针对关联飞书数据库所需的极微观细颗粒度的特殊数据表映射指引说明。
- **`requirements.txt`**：本系统 Python 底层库及所有周边组件列表依赖表单（包含高承载异步框架如 `fastapi`，`uvicorn`，强力重试插件如 `tenacity`，环境变量挂载 `python-dotenv`，基础网络调度框架 `requests`，原生非阻塞 `aiohttp` 等稳定必备底料构件）。
