# Aiduanju Engine (AI 短剧自动化生产流水线) — 部署与运行手册

> 版本：v3.0.0-PRO

本文档将指导您从零开始，在本地或服务器环境中部署本套 AI 短剧工业流水线系统。

---

## 1. 系统与环境要求

- **操作系统**：Windows 10/11 (推荐本地桌面端执行，以支持一键脚本) / Linux / macOS
- **Python 环境**：Python 3.10 及以上版本
- **核心底层构建**：
  - **FFmpeg**：**必须**安装。用于后期视频音频轨的重组混流，并且需加入系统的环境变量 `PATH`。
    - Windows 安装参考: `winget install ffmpeg`
  - **Aria2c**：**建议**安装。系统借助 Aria2c 进行视频文件的高速并发下载。
    - Windows 安装参考: `winget install aria2`

## 2. 拉取源代码与依赖安装

1. **拉取代码**：
   ```bash
   git clone https://github.com/XAIOxiao-guaisou/12345678910.git
   cd 12345678910
   git checkout 3.0.0
   ```

2. **安装 Python 依赖库**：
   建议在项目根目标下创建一个虚拟环境后执行：
   ```bash
   pip install -r requirements.txt
   ```

## 3. 环境变量配置 (.env)

系统高度依赖云端大语言模型与多媒体网关。请在项目根目录创建或拷贝 `.env` 文件，并根据您的实际情况配置以下核心参数：

```ini
# ==========================================
# 1. 飞书多维表格 Token (数据流转中枢)
# ==========================================
FEISHU_APP_ID="your_feishu_app_id"
FEISHU_APP_SECRET="your_feishu_app_secret"

FEISHU_APP_TOKEN_MEMORY="your_memory_table_token"
FEISHU_APP_TOKEN_ASSETS="your_assets_table_token"
FEISHU_APP_TOKEN_SCRIPT="your_script_table_token"
FEISHU_APP_TOKEN_FACTORY="your_factory_table_token"

# 表格名称绑定
FEISHU_TABLE_MEMORY="记忆中枢表"
FEISHU_TABLE_ASSETS="素材生成表"
FEISHU_TABLE_SCRIPT="剧本拆解表"
FEISHU_TABLE_FACTORY="制片工厂表"

# ==========================================
# 2. 大语言模型网关 (用于剧本拆解/分镜)
# ==========================================
DEEPSEEK_API_KEY="your_deepseek_api_key"

# ==========================================
# 3. 视频与生图网关 API
# ==========================================
# 阿里云百炼 (包含 Wan2.6 生图/生视频/TTS)
ALIYUN_API_KEY="your_aliyun_api_key"

# 极客智算 (多模网关兼容)
SEEDANCE_API_KEY="your_seedance_api_key"

# Pollinations 音效 (免费，可留空。若有高级API可替换)
```

## 4. 系统的运行与启动

本项目提供两种主要的启动方式：

### 方式一：Windows 一键启动体验 (推荐)
直接在项目根目录下双击运行 `一键启动WebUI.bat`。

该批处理脚本将会自动：
1. 校验本地的 Python 环境。
2. 在后台 `6800` 端口挂载启动 `aria2c` RPC 下载服务。
3. 启动 FastAPI 后台主引擎。
4. 自动打开浏览器跳转至 WebUI 界面（默认地址 `http://127.0.0.1:8000`）。

### 方式二：手动命令启动 (适用于服务器/Linux/macOS)
在终端中进入项目并在激活的 Python 环境下执行：

```bash
# 启动 FastAPI 后台引擎
uvicorn web.app:app --host 127.0.0.1 --port 8000 --env-file .env
```
随后请按需在另一个终端手动唤起 aria2c：
```bash
aria2c --enable-rpc --rpc-listen-all=true --rpc-allow-origin-all --rpc-listen-port=6800 --dir="Download"
```

## 5. 常规功能验证

1. 打开浏览器访问 `http://127.0.0.1:8000` 进入主面板。
2. 确保配置项中的网关与模型都已选中。
3. 在左侧面板上传带有小说文本的 `.txt` 文件。
4. 在右下角的【监控面板】即可看到 `SSE` 推流输出的实时分镜拆解及视频派发状态。
5. （可选沙盒测试）如果您只需要测通大模型拆解及飞书建表流程而不愿消耗视频代币，可勾选“启用内部沙盒模式”开关。

---
**附录**：如遇 FFmpeg 混装阶段报错，或者抛出 `[WinError 10061]` 无法连接的错误，请检查是否有后台 Python 程序卡死了 `8000` 端口并优先查验 `.env` API Key 是否处于欠费状态。
