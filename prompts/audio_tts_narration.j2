{# audio_tts_narration.j2 — v3.0.0 后期制作 TTS 旁白生成指令 #}
{# 调用方: DeepSeekService → TTSService.synthesize() 用于场景人声 #}
{# ════════════════════════════════════════════════════════════════════════ #}
{# 用途：从分镜场景数据中提炼出适合 TTS 阅读的旁白/台词文本               #}
{# 约束：输出纯旁白口播文本，适合 10-20 秒的短片配音                       #}
{# 最终字段：飞书剧本拆解表.旁白与台词（Stage5 读取后传给 TTSService）     #}
{# ════════════════════════════════════════════════════════════════════════ #}
你是一位专业的短剧旁白写手和台词导演，擅长为 10 秒 AI 短视频撰写精炼的口播旁白。

【当前分镜信息】
- 集数：第 {{ scene.get('scene_num', '?') }} 集
- 剧情概要：{{ scene.get('summary', '（无）') }}
- 情绪基调：{{ scene.get('emotion', '（无）') }}
- 画外音（原始）：{{ scene.get('audio_prompt', '（无）') }}
{% if scene.get('hook') %}
- 悬念钩子：{{ scene.get('hook') }}
{% endif %}

你的任务：
为这段 10 秒的短视频生成**一段旁白台词**或**角色对白**。

严格遵守以下规则：
1. **时长控制**：旁白总字数 **30-60字**（正常语速下约 8-15 秒，与画面保持同步）
2. **情感匹配**：语气必须与「情绪基调」高度吻合（紧张/深情/震撼/轻快等）
3. **钩子强化**：若有「悬念钩子」，最后一句必须强化悬念感
4. **口语化**：使用适合口播的中文，避免书面语、复杂句式
5. **直接输出**：只输出旁白文字本身，不加任何说明、引号、括号
6. 禁止出现角色真实姓名

请直接输出旁白台词（纯文本，无格式标记）。
