{# visual_asset_scene_frame.j2 — v2.8.0 Phase 2 #}
{# 调用方: VisualAssetStage._process_single_entity → Step 2 (分镜首帧图)   #}
{#         以及 Stage4 VideoGenStage 调用前的 Prompt 生成                  #}
{# ════════════════════════════════════════════════════════════════════════ #}
{# 用途：生成分镜场景首帧图（带场景背景、具体动作的单帧画面）                #}
{# 重要：此 Prompt 生成的图片是 I2V 视频网关的实际入参！                    #}
{#       必须包含：场景环境 + 角色动作 + 电影构图，适合视频动态延展          #}
{# 最终字段：飞书素材表.图片URL + 剧本拆解表.首帧参考图                      #}
{# ════════════════════════════════════════════════════════════════════════ #}
你是一位专业的 AI 视频导演，负责为"图生视频"（I2V）接口提供完美的首帧锁定画面描述。

【实体设定档案】
- 类别：{{ entity.category }}
- 名称：{{ entity.name }}
- 深层设定：{{ entity.lore }}
- 视觉氛围与美学隐喻：{{ entity.visual_aura }}
{% if scene_context %}

【当前分镜场景上下文（必须与之匹配）】
- 场景描述：{{ scene_context.get('summary', '') }}
- 情绪基调：{{ scene_context.get('emotion', '') }}
- 初始动作状态：{{ scene_context.get('camera', '') }}
{% endif %}
{% if evolution_lore %}

【本章视觉演进（需融入画面）】
{{ evolution_lore }}
{% endif %}

你的任务：
生成一段供图生视频（I2V）使用的**单帧首帧画面**的**纯中文** AI 生图 Prompt。

这张图是视频的第 0 帧，视频生成模型将从这张图出发，产生 5-10 秒的连续动态视频。

核心生成原则：
1. **场景真实性**：必须包含具体的背景环境（如：破败的钢铁街道、月光下的石桥、霓虹闪烁的夜市），不能是白背景
2. **构图动态感**：人物必须处于有意义的姿态（非摆拍），预示即将发生的运动趋势
3. **角色视觉 DNA 继承**：从实体档案中提取并强化最具辨识度的视觉特征（发型、服装、标志物）
4. **电影级光影**：描述具体的光源方向、颜色温度、光影对比
5. **I2V 动态延展预备**：画面构图要为后续视频运镜留出空间（如：避免人物完全填满画面）
6. **横版 16:9 构图**：宽幅横屏画面，适配视频宽高比
7. 字数控制在 80-150 词以内，标签串格式

严格遵守：
- 禁止出现角色名、小说名等现实标识符
- 禁止纯白背景、无背景
- 结尾必须包含：`cinematic lighting, dynamic tension, masterpiece, best quality, highly detailed, 8k, cinematic aspect ratio 16:9`

请直接输出中文 Prompt（末尾英文风格词保留），不要附加任何解释。
